/**
 * Utilities for Radix Agent Kit
 * 
 * Note: For address validation and derivation, use the Radix Engine Toolkit directly:
 * - Address validation: RadixEngineToolkit.Utils.* or appropriate validation methods
 * - Address derivation: RadixEngineToolkit.Derive.*
 * 
 * Example:
 * ```typescript
 * import { RadixEngineToolkit } from '@radixdlt/radix-engine-toolkit';
 * 
 * // Address derivation
 * const address = RadixEngineToolkit.Derive.virtualAccountAddressFromPublicKey(
 *   publicKey,
 *   NetworkId.Mainnet
 * );
 * 
 * // The toolkit also provides other address utilities and validation functions
 * ```
 */

// Export any other utility functions here
export {}; 